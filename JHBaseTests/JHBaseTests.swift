    import XCTest
    @testable import JHBase

    final class JHBaseTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertTrue(JHBase.kScreenWidth > 0, "Hello, World!")
        }
    }
